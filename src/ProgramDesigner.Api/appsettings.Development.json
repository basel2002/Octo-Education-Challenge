{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  // DEV-ONLY: list of origins allowed by the CORS policy.
  // Tighten or remove this section before deploying to production.
  "Cors": {
    "AllowedOrigins": [ "http://localhost:4200" ]
  }
}
