// Development environment — API URL points to the local .NET dev server.
// To change the API port, update applicationUrl in src/ProgramDesigner.Api/Properties/launchSettings.json
// and keep this value in sync.
export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:5173'
};
